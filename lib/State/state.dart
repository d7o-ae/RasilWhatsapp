import 'package:flutter/material.dart';

class RasilState extends ChangeNotifier {
// current user date
  DateTime currentDate = DateTime.now();

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}
